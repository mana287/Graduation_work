<div class="container-fluid">
  <div class="row">
    <%= render "shared/sidebar" %>

    <main class="col py-4">
      <div class="d-flex justify-content-between align-items-center mb-3">
        <h1 class="h5 mb-0">記事を編集</h1>
        <div>
          <%= link_to "← 記事詳細へ戻る", @article, class: "btn btn-sm btn-outline-secondary" %>
          <%= link_to "一覧へ", articles_path, class: "btn btn-sm btn-outline-secondary ms-2" %>
        </div>
      </div>

      <%= render "form", article: @article %>
    </main>
  </div>
</div>
